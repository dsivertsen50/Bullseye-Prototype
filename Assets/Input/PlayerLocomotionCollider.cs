using UnityEngine;

/// <summary>
/// Marks the invisible locomotion capsule so combat traces can ignore it.
/// Movement, grounding, and grenade radius checks may still use this collider.
/// </summary>
public class PlayerLocomotionCollider : MonoBehaviour
{
}
